[web]
# CI generates its own inventory from `terraform output`.
# For manual runs, replace the example below with the current EC2 public IP.
# 203.0.113.10 ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/id_rsa
